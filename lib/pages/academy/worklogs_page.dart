import 'package:flutter/material.dart';

class WorklogsPage extends StatelessWidget {
  const WorklogsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Worklogs Page"));
  }
}
