import 'package:flutter/material.dart';

class StudentWorklogsPage extends StatelessWidget {
  const StudentWorklogsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Student Worklogs Page"));
  }
}
