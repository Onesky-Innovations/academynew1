import 'package:flutter/material.dart';

class StudentFeesPage extends StatelessWidget {
  const StudentFeesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Student Fees Page"));
  }
}
